import dotenv from 'dotenv'; dotenv.config(); import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('properties').select('title, price, latitude, longitude, address, googleMapsUrl').then(({data}) => console.log(JSON.stringify(data.filter(p => p.price === 215000), null, 2)));
